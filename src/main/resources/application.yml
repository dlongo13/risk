spring:
 datasource:
  url: jdbc:hsqldb:mem:risk
  username: sa
  password:
  driverClassName: org.hsqldb.jdbcDriver

 jpa:
  database: HSQL
  show-sql: true
  hibernate:
   ddl-auto: create-drop
   dialect: org.hibernate.dialect.HSQLDialect